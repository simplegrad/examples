This is a repo with examples for simplegrad

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install ipykernel  # for vscode, if using juputer consider installing jupyterlab
pip install "simplegrad[visual] @ git+https://github.com/simplegrad/simplegrad.git"
```