This is a repo with examples for `simplegrad`

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
# register the kernel
python -m ipykernel install --user --name=sg-env --display-name "Python (sg-env)"
```